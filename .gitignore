# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
.venv/
venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# ─── User data & credentials (NEVER commit) ───────────────────────────────
# All user-specific files are stored in data/ during development.
# Passwords are in macOS Keychain (not a file), never committed.
# Positions / snapshots (personal financial data)
data/
*_positions.json
*positions.json
positions.json
# SQLite snapshots (contain portfolio history)
*.db
# Cache files that may contain ticker/price data
*_cache.json
beta_cache.json
# Environment / secrets
.env
.env.local
.env.*
secrets.toml
config.local.toml
# ──────────────────────────────────────────────────────────────────────────

# OS
.DS_Store
Thumbs.db
