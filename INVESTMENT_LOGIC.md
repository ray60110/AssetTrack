# AssetTrack 投資判斷邏輯技術文件

本文件說明 AssetTrack TUI 目前的「投資判斷」邏輯：四大分析功能各自如何從自己的面向產生投資建議、每項建議如何以歷史資料回測驗證、回測本身的統計可信度如何評估、參數如何依回測結果定期校準（需使用者確認才套用），以及如何把四個面向統整成主頁上的一張跨模型總結建議。適用範圍為 **美股**；台股／TWD 僅保留於持倉追蹤層，一律不進入任何投資建議。

對應的追蹤項目為 bug#00090–00113（詳見 `action_buglist/bug_feature_tracking.md`）。

---

## 一、設計原則（所有判斷共通的底線）

整套投資判斷建立在幾條不可違反的紀律上，理解這幾條才能正確解讀畫面上的每一個數字。

**只用真實累積的本機快照，離線運算，不回填、不臆測。** 系統不對缺資料的日子做任何估計。ETF 持股、期權鏈、類股成分股都是每日在背景刷新時「一天一筆真實快照」逐日累積在本機（`data/etf_cache/history/*.jsonl`、`options_cache/history/*.jsonl`、`sector_cache/history/*.jsonl`），所有分析與回測都是零網路、純本機讀這些快照運算。沒有真實資料時，畫面誠實顯示「資料收集中／資料累積中」，而不是生成看似可信的假結論。這也是為什麼功能剛上線時回測命中率必然為 0——那正是要讓使用者「隨時知道」的狀態。

**本機資料保留 365 天。** 所有供分析／回測用的下載資料，保留窗統一為 365 天（`storage.ANALYSIS_CACHE_RETENTION_DAYS = 365`，單一常數，套用於 `cleanup_old_etf_caches` 與 `prune_etf_history` / `prune_options_history` / `prune_sector_history`）。保留窗越長，walk-forward 回測可用的真實樣本越多。此為「本機保留天數」，與「多久重新抓取一次」的 in-memory 新鮮度 TTL（beta／無風險利率／FRED 6 小時、Fed 期貨 1 小時、報價 60 秒）是兩回事。

**登入後即開始抓取，狀態列持續顯示進度。** 使用者成功登入後 `AssetTrackApp._start_dashboard` 會立即啟動一次背景抓取（`_background_data_refresh`），不必等 30 分鐘週期；之後每 30 分鐘依快取新鮮度冪等地補抓。主頁底部常駐狀態列（`#status-bar`）每秒從 `_fetch_activity` 讀取，顯示目前背景正在抓什麼（如「⏳ 正在抓取：主動式ETF持股（12 檔）、期權鏈（5 檔標的）」），閒置時顯示「✓ 資料已是最新（背景閒置）」。

**投資建議一律以美股為主，台股全數排除。** 判定台股的唯一來源是 `shared.is_taiwan_position(p)`（幣別 TWD、代碼 .TW/.TWO 結尾、或 market == "TW"，與 `models.Position` 內建 is_tw 口徑一致）。台股在下列所有投資建議路徑被排除：部位立場交叉比對（`position_stance_by_symbol`）、期權淨 Greeks 分桶（`compute_portfolio_greeks`）、期權觀察清單標的來源（`_watchlist_underlyings`）、ETF 趨勢共識宇集（僅 `US_ACTIVE_TICKERS`）、ETF 排行頁（已移除台股 tab）、類股成分股（`summarize_group` 排除 .TW/.TWO）、以及跨模型總結。**台股／TWD 的持倉追蹤、報價、USDTWD 匯率換算、基準幣別切換、代碼推斷（2330→TW/TWD）一律保留照常運作**——只有「投資建議」層剔除台股。

---

## 二、四大功能各自的投資判斷邏輯

### 1. 近期重大事件與重點經濟指標動態解析（bug#00102）

此功能整合三類事件與重點總經動態：
1. **個股與成分股財報日程**：持有部位個股與 SOX 十大半導體成分股的財報日期（`quotes.fetch_earnings_calendar`）。
2. **總經數據公佈日程**：FED/NFP/CPI 三大總經數據的公佈日程（`shared.get_upcoming_macro_events`，自動換算美東→台灣時區）。
3. **重點經濟指標期對期變動比較與動態解析**（`quotes.fetch_latest_macro_readings` + `shared.format_macro_analysis_lines`）：追蹤核心 CPI、核心 PCE、NFP 非農就業、失業率與聯邦資金利率五大指標。系統取得 FRED 最新數值時，同步比較上期數據（計算月增率變動量 `mom_change_pp` / 人數變動 `change_diff` / 百分點變動 `change_pp`），並生成動態經濟意涵解析（例如：核心 CPI 月增率較上期放緩 0.2pp，解析「通膨壓力持續減緩，為央行貨幣政策提供降息與維持寬鬆空間」；若回升則解析「粘性通膨反彈壓力，降息與寬鬆預期可能延後」）。

依使用者決策，此項主要作數據比對與經濟解析，不強行硬湊多空單邊點位、亦不做方向回測。它的角色是「行事曆與重點總經指標趨勢解析」。在跨模型總結中作為「謹慎度修正」——近日若有重大總經事件即提示降低把握（見第五節）。

### 2. 主動式 ETF 排行與趨勢共識（`analysis.py`，bug#00103 / bug#00104 擴充）

投資建議來自三個維度，皆讀取逐日真實 ETF 快照（包含個股持股與 `asset_classes` 資產配置），於 **14 天緊湊視窗內**比較「視窗兩端的代表快照」——為降低單一異常端點快照翻轉訊號的脆弱度，兩端各取 `k = min(3, 視窗內天數//2)` 筆快照的**中位數**（權重／持股價／AUM）作代表值，僅 2 筆時退化為單純「最早 vs 最新」兩點比較（`_endpoint_view`，bug#00110）；視窗對齊期權觀察 14 天與跨模型預測區間 14 天：

1. **大類資產輪動共識（資產配置層級）** 由 `compute_asset_class_trends` 計算：追蹤各大主動式 ETF 在特定區間內（14 天），是否普遍在**增持或減持股票 (Stock)、債券 (Bond)、現金 (Cash)、特別股 (Preferred)、可轉債 (Convertible)、黃金/大宗商品/其他 (Gold/Commodities/Other)**。若有 ≥ `consensus_threshold`（預設 0.5）比例的基金同向調整該資產類別，即生成大類資產輪動建議（例如：`🌐 【大類資產輪動】75% 主動式 ETF 同步增碼「股票 (Stock)」，減碼「債券與現金」▶ 機構資金轉向風險配置/加碼`）。
2. **個股多數性共識（個股同時買入/賣出層級）** 由 `compute_symbol_trends` 計算：對每一檔被持有的股票，判斷各檔 ETF 是增碼、減碼還是持平。關鍵紀律是**兩個獨立的真實訊號必須同向**——真實股數變化（由各快照當日的真實 AUM×權重÷真實持股價反推）與權重變化（門檻 `flat_threshold_pp` 0.5 個百分點）都往上，才算「增碼」；都往下才算「減碼」；任一缺席或方向不一致一律歸為「持平」、不計入。跨所有已就緒的 ETF，某檔股票的同向基金比例需 ≥ `consensus_threshold`（預設 0.5）**且嚴格多於反向**（up 需 `pct_up > pct_down`、down 需 `pct_down > pct_up`）才產生同時買入/賣出建議（`🟢 【同時買入】` / `🔴 【同時賣出】`）；「增碼檔數＝減碼檔數」的平手一律歸中性 mixed、不偏多（bug#00107）。同一判定套用於 `compute_asset_class_trends` 的大類資產共識。
3. **規模性（單一大額部位）** 由 `rank_scale_events` 計算：刻意「不」套用跨基金共識門檻，讓單一基金的大額真實部位變動（`💰 【規模性大額變動】`）也能被看見，門檻為「絕對金額 ≥ $5M」且「相對該基金 AUM ≥ 0.5%」雙重條件（避免小基金雜訊）。

結論會與使用者自身部位方向交叉比對（`position_stance_by_symbol`），標註「與你偏多的部位一致／相反／你尚未持有可留意」等建設性提示。

### 3. 期權觀察清單方向結論（`options_analysis.py` + `calibration.py`，bug#00105 擴充）

觀察標的為「持倉 ∪ 使用者自訂清單」（`_watchlist_underlyings`，已排除台股）。每檔標的的「綜合方向結論」（看多／看空／觀望）由 `compute_directional_verdicts` 產生，判斷依據為兩個真實訊號：

1. **Dollar Open Interest (Dollar Delta OI) Skew**（bug#00105 優化）：取消舊有純「合約張數」計算方式，改採 **Delta 權重名義金額曝光**（$\text{Dollar Delta OI} = \Delta\text{OI} \times \text{Spot} \times |\text{Delta}| \times 100$）。有效防止數百張低價末日 Call（如 $0.05 美金的遠價末日合約）以合約張數扭曲真實機構資金方向，確保僅有真正具權重曝光金額的籌碼集中在買權（call_pct ≥ 70%）或賣權（≤ 30%）時才計為多空方向訊號。
2. **重定價殘差偏向**：扣除 delta/gamma(物理凸性)/theta(時間流逝)/DTE 後的淨重定價殘差 bias——不再只扣一階 delta，而是以合約在 t0 的隱含波動率為基準，用 Black-Scholes 在「新現價、**當日縮短後的到期天數（DTE）**、IV 維持不變」下重新定價，殘差 = 實際權利金 − 該理論價（`_repricing_decomp`，bug#00097）。如此 **delta、gamma（物理凸性）、theta（時間流逝）與每日不同的 DTE 全部被精確扣除**，殘差僅剩由 IV／需求驅動的重定價（買權殘差為正＝偏多力量、賣權殘差為正＝偏空力量）。**淨方向 bias 以各合約未平倉量 (OI) 為權重取加權平均**（每股美元，缺 OI 退回等權 1；`_residual_bias`，bug#00108）——避免單一高價或極低 OI 合約主導整檔標的。方向門檻相對現價正規化：`|bias| ≥ max(bias_min_abs=0.15, bias_min_pct=0.03%×現價)` 且參與合約數 ≥ 2 才計為 ±1（bug#00108），使 $600 與 $15 的標的不再共用同一絕對美元門檻。

**skew 買/賣方向交叉確認（bug#00109）**：未平倉建倉本身無法分辨買方或賣方發起，故子訊號 1 的 skew 需子訊號 2 的**同側殘差**確認——買權集中但買權側 OI 加權殘差為負（多屬賣出買權）不確認偏多、賣權集中但賣權側殘差為負（賣出賣權＝偏多）不確認偏空，此時 skew 不計入方向並標 `skew_unconfirmed`。

兩子訊號各給 +1／0／−1 分後相加，>0→「多」、<0→「空」、=0→「觀望」；兩者同時非零且方向相反時另標 `conflict=True`（不給方向）。視窗為 14 天。輸出由 `generate_verdict_cards`（單則）與 `generate_grouped_analysis_card`（逐標的分組，Dashboard 卡片與觀察清單頁共用、每檔附自己的獨立回測命中率）呈現，並附 IV 位階與財報降權提示。

### 4. 類股板塊共識（`sector_analysis.py`）

抓「整個族群普遍同向」而非單一名股在動。核心是**廣度擴散指數**：廣度 = (上漲成分股數 − 下跌成分股數) / 有報價成分股數，範圍 −1…+1。搭配**市值加權報酬**（Σ 權重×報酬，同時就是該板塊的「市值漲跌%」）。某日判為「普遍上漲」需**兩訊號一致**：廣度 ≥ `breadth_threshold`（預設 0.5）且市值加權報酬 > `capw_threshold`（沿用 ETF「兩個真實訊號需同向」的紀律）。再加**持續性過濾**（`detect_broad_flow`）：最近 `lookback`（5）個交易日中有 ≥ `min_days`（3）天同向普遍走勢，才在畫面標記，用以區分「持續共同買賣」與「單日雜訊」。輸出由 `generate_sector_conclusions` 呈現。成分股於進入運算前即排除台股（`summarize_group`）。

---

## 三、回測層：每項建議如何被歷史驗證

三項有方向的功能（ETF／期權／類股；事件依決策不做回測）共用同一套 **walk-forward** 骨架，各自實作於 `analysis.backtest_etf_consensus`、`calibration.backtest_verdicts`、`sector_analysis.backtest_sector_flow`。

作法：把每一個歷史日 T「當作當下」，**只用 ≤ T 的真實快照**重新推導當日結論，再看該標的在 T 之後 ≥ h 天的真實走勢是否與結論方向一致（命中）。三項共同的關鍵設計：

- **同一函式原則**：回測呼叫的就是畫面用的同一個判斷函式（ETF 用 `compute_symbol_trends`、期權用 `compute_directional_verdicts`、類股用 `detect_broad_flow`），沒有「回測一套、畫面另一套」的兩套標準問題。
- **無前視偏誤**：每個 T 只餵 ≤ T 的資料切片，結構上保證不看未來。
- **前瞻期 1／5／10／14／30／60 天並列**（`DEFAULT_HORIZONS`，bug#00106）：看得出訊號偏短線還是波段有效（14 天為跨模型總結對齊之預測區間；30／60 天供機構調倉真實發酵之長線觀察與校準依據）。
- **前瞻報酬的量測對象**：ETF 用「該股票跨 ETF 真實持股價中位數」，期權用「標的自身快照裡的 spot_price」，類股用「該板塊每日市值加權報酬複利成的類指數」。
- **命中 vs 基準**：命中率與「同宇集基準上漲率」相比得出超額（edge）。單看命中率沒有意義——若大盤本來 60% 的日子在漲，一個「永遠看多」的訊號也有 60% 命中率卻零技能，所以每個命中率旁一定附基準與 edge。
- **誠實門檻**：可評估訊號數 < `min_signals`（20）時標「資料累積中」，不顯示看似可信的數字。

回測結果就地顯示於每則結論尾端，例如：`▶ 回測：前瞻5日同向共識命中率 72%（n=120，超額 +17pp）…`。

---

## 四、回測「本身」的統計驗證層（`backtest_stats.py`）

命中率也有準確性問題——它是真本事還是運氣？此層把三套回測共用的 `by_horizon` 聚合值轉成可判讀的統計，三套回測回傳前皆呼叫 `attach_significance` 一併算好。

**Wilson score 信賴區間**（`wilson_interval`）：命中率的 95% 區間，小樣本下比常態近似穩健。

**對基準的單尾二項檢定**（`binom_sf` 精確 log-space 計算 + `direction_significance`）：虛無假設 H0 為「命中率 = 基準上漲率」（無技能），對立為「命中率 > 基準」。並對「多個前瞻期 × 多空」的多次檢定做 **Bonferroni 多重比較調整**（`significant_adj`，門檻 0.05 / 檢定次數），避免因為挑最好看的一組而高估顯著性。

**前後子區間穩定性**（`_stability`）：把逐訊號依日期切前後兩半，看命中率是否**兩段都贏過無技能基準**（依各半段方向組成加權：up→基準上漲率、down→1−基準上漲率；缺基準時退回 0.5，bug#00112）；只在一段贏過基準的訊號標為不穩定（近似樣本外檢查，防非平穩／單一盤勢過擬合）。舊版以固定 0.5 判定，會把基準本就 60%、命中僅 52% 的劣訊號誤標為「前後一致」，故改對基準。

這三者讓畫面上呈現的不只是「命中率幾 %」，而是升級成「95%CI 63–79%，基準 55%，顯著優於基準(p=0.000)；前後子區間一致」這類可信度總結（`significance_phrase` 就地顯示、`validation_label` 供校準狀態列）。

**二項檢定與有效獨立樣本數（ESS）優化（bug#00106）**：為了解決連續每日重複訊號在長前瞻期（如 14/30/60 天）下重疊視窗報酬高度自相關導致 $p$ 值虛假過高（顯著性高估）的問題，二項檢定一律計算**有效獨立樣本數** ($\text{ESS} = \max(1, \lfloor N / \text{Horizon} \rfloor)$)，並以 $\text{hits}_{\text{ess}} = \text{round}(\text{hit\_rate} \times \text{ESS})$ 輸入二項檢定與 Wilson CI。這確保即使在累積數百筆每日訊號時，統計檢定仍能嚴格消除重複視窗的自相關偏差，給出真實保守的顯著性評估。

---

## 五、跨模型總結建議（`cross_model.py`）

主頁最上方一張「🧭 跨模型總結建議」，把三項有回測背書的方向訊號統整成一個整體傾向。

跨模型的**預測區間統一設定為 14 天**（`TARGET_HORIZON = 14`，bug#00097）。三項模型原生時間尺度不同，為了在同一時間維度上比較，三套回測的前瞻期一律擴充為 1／5／10／14／**30**／**60** 天（`DEFAULT_HORIZONS`，bug#00106），而**每項在跨模型中的可信度權重一律取「14 天前瞻期」的顯著性**——某模型若在 14 天前瞻樣本不足或不顯著，即在總結中降權或棄權（`_reliability(backtest, horizon=14)`）。長線 30/60 天前瞻期提供機構調倉真實發酵時間的顯著性觀察與校準依據。

每項先算一個**淨方向分數**（多−空正規化到 −1…+1）：ETF 取共識為 up/down 的標的數淨值、期權取方向為多/空的標的數淨值、類股取 flow 為 up/down 的族群數淨值。再以**該項在 14 天前瞻期的回測可信度加權**——可信度直接沿用第四節的統計驗證（`_reliability`）：**就緒須同時滿足原始 n≥20 與有效獨立樣本數 ESS≥3**（bug#00111，避免長前瞻期下 ESS 被自相關砍到 1 時仍以「1 個有效樣本」給權重、此時 Wilson CI 近乎 (0,1)），否則權重 0（該項這次棄權，不硬湊方向）；達就緒後未顯著 → 0.2、顯著（未過多重檢定）→ 0.5、顯著且過多重比較調整 → 1.0。整體分數 = 加權平均，映射為「強烈偏多／偏多／中性觀望／偏空／強烈偏空」，並依有幾項達顯著給出把握度（高／中／低）。所有可信權重為 0 時誠實回報「資料累積中」，不給假方向。

「近期重大事件」不投方向票（維持資訊性），改作**謹慎度修正**：近 3 日內若有 FED／CPI／NFP 等重大總經事件，提示「降低單邊把握、等待數據塵埃落定」（`_event_caution`）。

卡片同時列出每項的貢獻與權重，例如：`主動式ETF共識：偏多 6多/1空，權重 1.00（顯著）`，讓總結完全可回溯。

---

## 六、每雙週／每週自動校準（`calibration_schedule.py`）

系統具備「定期依回測結果修正投資建議門檻」的能力，但**永不自行套用，一律需使用者按確認**。

**時間點：預設每雙週（`cadence_days = 14`），可切每週（7）。** 選雙週是因為在跨模型對齊之 14 天前瞻期下，一週的最新訊號多半尚未結算，且有效樣本受自相關拖累——每週重調多屬雜訊，雙週看到的已結算證據約翻倍才有意義。「重算＋顯示」是每次刷新持續進行的；只有「提出需確認的調整」設為雙週事件。

**提案一律用第四節的統計驗證把關**（`propose_adjustments`）：樣本不足不提案；樣本充足但「未顯著優於基準」→ 建議收緊主要門檻一步（更嚴格篩選）；「已顯著有效但前後子區間不一致」→ 保守收緊；「已顯著有效且門檻高於預設」→ 放寬一步回預設；皆以上下限夾住。可調參數為 ETF 的一致門檻／最少檔數、類股的廣度門檻／持續天數（`PARAM_SPEC`）。

**流程**：背景抓完最新快照後，`_maybe_recalibrate` 判斷是否到期；到期才用最新快照重跑回測、把提案存入 `pending` 並蓋上「上次校準日」（`run_recalibration`，**絕不自動套用**），並跳通知「⚙️ 有 N 項投資建議校準待你確認」。使用者按 **k** 開啟 `CalibrationModal`，可【套用調整】（`apply_pending`，此時才寫入 `active_params` 並記 history）、【略過建議】（`dismiss_pending`）、【立即重算】或【切換週期】。狀態存於 `data/{user}_calibration.json`。套用後，主頁三張卡片與相關頁面透過 `_active_params(user)` 讀取新門檻，建議立即以新設定重算。

---

## 七、模組對照表

| 模組 | 職責 |
|---|---|
| `assettrack/analysis.py` | 主動式 ETF 多數性/規模性共識 + `backtest_etf_consensus` 回測 |
| `assettrack/options_analysis.py` | 期權方向結論（Dollar Delta OI skew ＋同側殘差確認 ＋ OI 加權重定價殘差）、淨 Greeks、結論卡 |
| `assettrack/calibration.py` | 期權方向結論的 walk-forward 回測 + 校準狀態標籤 |
| `assettrack/sector_analysis.py` | 類股廣度共識 + `backtest_sector_flow` 回測 |
| `assettrack/shared.py` | 總經事件排程、部位立場、`is_taiwan_position`（投資建議台股判定唯一來源） |
| `assettrack/backtest_stats.py` | 回測統計驗證：Wilson CI、對基準二項檢定（多重比較調整）、子區間穩定性 |
| `assettrack/calibration_schedule.py` | 每雙週/週自動校準引擎（提案顯著性把關、需確認才套用） |
| `assettrack/cross_model.py` | 跨模型總結：三方向以回測可信度加權、事件作謹慎度修正 |
| `assettrack/storage.py` | 逐日真實快照持久化、365 天保留、校準狀態檔 |
| `assettrack/tui.py` | 全螢幕 TUI：卡片/頁面、狀態列、登入後抓取、校準對話框（k） |

## 八、關鍵預設參數

| 面向 | 參數 | 預設 | 說明 |
|---|---|---|---|
| ETF | 分析視窗 | 14 天 | `compute_symbol_trends` / 回測（bug#00104） |
| ETF | 端點取樣 | 兩端各 k=min(3, 天數//2) 筆中位數 | 穩健化端點、降雜訊翻轉（bug#00110） |
| ETF | `consensus_threshold` | 0.5 | 多數性一致門檻；需嚴格多於反向、平手歸中性（bug#00107，**可校準**） |
| ETF | `min_etfs_evaluated` | 4 | 最少評估檔數（**可校準**） |
| ETF | `flat_threshold_pp` | 0.5 | 權重變化持平門檻（百分點） |
| 期權 | 分析視窗 | 14 天 | `compute_directional_verdicts` |
| 期權 | 殘差方法 | BS 重定價分解＋OI 加權平均 | 扣除 delta/gamma/theta/DTE；以 OI 加權平均（bug#00108） |
| 期權 | `bias_min_pct` | 0.03（%×現價） | 殘差方向門檻＝max(0.15, 0.03%×現價)，相對現價正規化（bug#00108） |
| 期權 | skew 確認 | 同側重定價殘差 | skew 需同側殘差確認買/賣方向，否則不計入（bug#00109） |
| 跨模型 | 預測區間 | 14 天 | 各模型可信度統一於此前瞻期評估 |
| 跨模型 | 就緒門檻 | n≥20 且 ESS≥3 | 給權重前的最低樣本（bug#00111） |
| 類股 | `breadth_threshold` | 0.5 | 廣度門檻（**可校準**） |
| 類股 | `capw_threshold` | 0.1 | 市值加權報酬門檻 |
| 類股 | `lookback` / `min_days` | 5 / 3 | 持續性視窗與最少天數（min_days **可校準**） |
| 回測 | 前瞻期 | 1 / 5 / 10 / 14 / 30 / 60 天 | 六組並列；14 天為跨模型對齊之預測區間（bug#00106） |
| 回測 | `min_signals` | 20 | 低於此標「資料累積中」 |
| 統計 | 顯著水準 | 0.05（多重比較後 0.05/檢定數） | 二項檢定 |
| 校準 | `cadence_days` | 14（雙週；可切 7） | 提案頻率 |
| 保留 | `ANALYSIS_CACHE_RETENTION_DAYS` | 365 天 | 本機分析資料保留窗 |

---

## 九、限制與正確解讀

系統刻意偏保守、偏誠實。功能剛上線或資料視窗有大段空缺時，回測與跨模型總結都會顯示「資料累積中」而非假結論——這是設計，不是故障，需要持續使用讓真實快照逐日累積。回測命中率受序列自相關影響，實際有效樣本小於 n，故顯著性判定一律偏保守，並輔以多重比較調整與前後子區間穩定性作為部分防禦。所有方向結論都是「基於歷史統計的機率性判斷」，非保證；跨模型總結的把握度標示（高/中/低）正是要讓使用者知道當下證據有多強。

---

## 十、2026-07 邏輯審查後的修正與決策（bug#00107–00113）

使用者要求逐一檢視四大功能建議邏輯與回測模型的優化空間，共列 10 點。其中 6 點已改為程式碼並**併入上述各節內文**（本節不再重述細節，僅列對照）；其餘 4 點經評估維持現行設計，理由記於下方。

**已修正並併入內文（bug#00107–00112）：**

| # | 修正 | 內文位置 |
|---|---|---|
| 1 | ETF 多數性平手歸中性、不偏多（`compute_symbol_trends`／`compute_asset_class_trends`） | 第二節 2 |
| 2 | 期權殘差改 OI 加權平均、方向門檻相對現價正規化（`_residual_bias`／`compute_directional_verdicts`） | 第二節 3、第八節 |
| 3 | 建倉 skew 以同側重定價殘差交叉確認買/賣方向（`skew_unconfirmed`） | 第二節 3 |
| 4 | ETF 兩端改取各 k 筆中位數之穩健代表值（`_endpoint_view`） | 第二節 2、第八節 |
| 5 | 跨模型就緒門檻加入 ESS≥3，與顯著性口徑一致（`_reliability`） | 第五節、第八節 |
| 7 | 前後穩定性改對無技能基準判定，非固定 0.5（`_stability`） | 第四節 |

> 期權端（`_residual_bias`／skew／IV 背離）的「端點中位數」同型穩健化尚未做，列為後續。

**維持現行設計（已評估，記錄理由）：**

6. **期權兩子訊號（skew／殘差）維持等權**：不改為依各自回測可信度加權，因為那會破壞「結論卡顯示的邏輯與被回測的邏輯必須是同一個函式」的核心紀律（見 `options_verdict_card.md`）；矛盾時的 conflict／skew 未確認機制已提供必要的保護。

8. **「近期重大事件」維持資訊性、不投方向票**：依既有使用者決策（第二節 1），總經數據解析不硬湊方向；`_event_caution` 維持前瞻式謹慎提示。將「事件剛公佈後的消化期」與「總經數據作軟性傾向」列為未來可選增強，需接線 TUI 並改變事件定位，不在本次範圍。

9. **類股回測目標（前瞻市值加權報酬）維持**：訊號含 `capw>0`、回測目標為前瞻 capw 複利指數，兩者同源會使命中率略含動能自相關成分——這是任何動能訊號的內生性質，且已由「基準＋超額（edge）」與 ESS 部分抵銷；改變預測目標反而失去「預測該類股是否續漲」的原意。

10. **二項檢定的 Bonferroni 多重比較維持跨前瞻期校正**：跨模型雖只取 14 天，但畫面同時呈現多個前瞻期，對全體有樣本的前瞻期做校正是正確的保守作法；無樣本的前瞻期本就不計入檢定數，不會虛增懲罰。維持現行以貫徹「偏保守」原則。

> 驗證方式：每次改動皆經 `py_compile`、合成資料單元測試、以及 `scripts/verify_tui.py` Textual 無頭掛載測試。
